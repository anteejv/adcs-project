[Unit]
Description=My nice service
After=network-online.target

[Service]
Type=simple
User={{app_user}}
ExecStart=/usr/bin/java -jar {{app_full_path}}
Restart=always

[Install]
WantedBy=multi-user.target